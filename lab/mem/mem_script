./67b306 &
pid=$(pidof 67b306)
delay=30

for i in $(seq $delay) ; do
        echo -n $i " " >> mem_graph.dat
        top -bn1 -p $pid | grep $pid | awk '{print " " $5 " " $6 }' >> mem_graph.dat
        sleep 0.1;
   done
   python3.9 mem_replace.py
   gnuplot mem.gp > mem.png

kill -9 $pid
